import { renderHook } from '@testing-library/react-hooks';

import useThrottle from './useThrottle';

describe('useThrottle CSR', () => {
  it('does this', () => {
    // const renderedHook = renderHook(() => useThrottle());

    expect(true).toBeTruthy();
  });
});
