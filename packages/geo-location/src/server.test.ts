/**
 * @jest-environment node
 */

describe('useGeoLocation SSR', () => {
  it('does this', () => {
    // const renderedHook = renderHookServer(() => useGeoLocation());

    expect(true).toBeTruthy();
  });
});
