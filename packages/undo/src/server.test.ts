/**
 * @jest-environment node
 */

import renderHookServer from '../../../utils/renderHookServer';

import useUndo from './useUndo';

describe('useUndo SSR', () => {
  it('does this', () => {
    // const renderedHook = renderHookServer(() => useUndo());

    expect(true).toBeTruthy();
  });
});
