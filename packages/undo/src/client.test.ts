import { renderHook } from '@testing-library/react-hooks';

import useUndo from './useUndo';

describe('useUndo CSR', () => {
  it('does this', () => {
    // const renderedHook = renderHook(() => useUndo());

    expect(true).toBeTruthy();
  });
});
