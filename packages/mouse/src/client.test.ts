import { renderHook } from '@testing-library/react-hooks';

import useMouseRegion from './useMouseRegion';

describe('useMouseRegion CSR', () => {
  it('does this', () => {
    // const renderedHook = renderHook(() => useMouseRegion());

    expect(true).toBeTruthy();
  });
});
