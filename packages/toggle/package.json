{
  "author": "Devin Metivier @devjmetivier",
  "bugs": {
    "url": "https://github.com/devjmetivier/hookit/issues"
  },
  "description": "Toggle a boolean state",
  "devDependencies": {
    "eslint-config": "*",
    "tsconfig": "*"
  },
  "files": [
    "cjs",
    "esm",
    "types"
  ],
  "homepage": "https://github.com/devjmetivier/hookit/tree/master/packages/toggle#readme",
  "keywords": [
    "react",
    "hooks",
    "toggle",
    "boolean"
  ],
  "license": "MIT",
  "main": "cjs/index.js",
  "module": "esm/index.js",
  "name": "@hookit/toggle",
  "peerDependencies": {
    "react": ">=16.8"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/devjmetivier/hookit.git"
  },
  "scripts": {
    "build": "yarn build:esm && yarn build:cjs && yarn build:types",
    "build:cjs": "ncc build src/index.ts -o cjs -m -e react",
    "build:esm": "tsc --target ESNext --module ES6 --outDir esm",
    "build:types": "tsc --project tsconfig.types.json",
    "build:watch": "tsc-watch -p ./tsconfig.json --onSuccess 'yarn build'",
    "clean": "rm -rf node_modules && rm -rf .turbo && rm -rf esm/ && rm -rf cjs/ && rm -rf types/ && rm -rf dist/",
    "lint": "eslint . --ext .js,.ts,.jsx,.tsx",
    "prebuild": "yarn clean",
    "prepublishOnly": "yarn build"
  },
  "types": "types/index.d.ts",
  "version": "0.2.0"
}
